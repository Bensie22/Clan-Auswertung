# === Automatisch generierte CSV-Exporte ===
# Diese werden bei jedem Run neu erstellt und gehören nicht ins Repo.
# Nur die neueste Datei zählt – alte verfälschen die Auswertung.
uploads/*.csv
uploads/archiv/

# === Archivierte HTML-Berichte ===
# index.html im Root bleibt (GitHub Pages), die archivierten Kopien nicht.
output/auswertung_*.html
output/archiv/

# === Lokale Entwicklungsumgebung ===
.env
__pycache__/
*.pyc
*.pyo
.DS_Store
Thumbs.db
